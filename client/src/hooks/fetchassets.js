const useFetchAssets = () => {
    return async (user) => {
        let assets = [];
        console.log(assets);
        console.log('done');
        return assets;
    }
}

export default useFetchAssets;